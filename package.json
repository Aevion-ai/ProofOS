{
  "name": "proofos",
  "version": "1.0.0",
  "private": true,
  "description": "ProofOS — receipt chain for Lean 4 proof obligations",
  "scripts": {
    "test": "npx playwright test --reporter=line",
    "test:headed": "npx playwright test --headed",
    "lint:ts": "eslint e2e playwright.config.ts",
    "lint:css": "stylelint '**/*.css' --ignore-path .stylelintignore"
  },
  "devDependencies": {
    "@playwright/test": "1.61.1",
    "@typescript-eslint/eslint-plugin": "8.65.0",
    "@typescript-eslint/parser": "8.65.0",
    "eslint": "8.57.0",
    "typescript": "5.5.4"
  }
}
